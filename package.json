{
  "repository": {},
  "scripts": {
    "build": "rm -rf dist; mkdir dist; watchify -v -t babelify main.js -o dist/main.js",
    "serve": "live-server --entry-file=index.html"  
  },
  "dependencies": {
    "@cycle/dom": "*",
    "@cycle/isolate": "*",
    "@cycle/xstream-adapter": "*",
    "@cycle/xstream-run": "*",
    "cyclic-router": "*",
    "history": "^3.0.0",
    "immutable": "*",
    "xstream": "*",
    "switch-path": "*"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-plugin-transform-object-rest-spread": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.3.0"
  }
}
